import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'tunisia-forum',
  webDir: 'dist/TunisiaForum'
};

export default config;
